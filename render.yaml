services:
  - type: web
    name: SISTec IoT Application 2026
    env: node
    buildCommand: "npm install"
    startCommand: "npm start"
    envVars:
      - key: NODE_ENV
        value: production
    plan: free